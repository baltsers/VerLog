**App Description**: Transfer files seamlessly between all your devices - Snapdrop.
**Entry Method Signature**: <com.fmsys.snapdrop.MainActivity: void onRestart()>
**Method Line Number(start,end)**: (367, 373)
**Commit Messages between two versions**:
	1. make links clickable in attribution section
	2. add Material Design 3 for the app icon
	3. Update translation_validation.yaml
	4. New Crowdin updates (#216)
	5. Update README.md
	6. some cleanup
	7. New Bugfix Release - v1.11.1
**Partial call graph**:
	- **Nodes(Reachable methods(and their changes) from entry method)**:

		0. <com.fmsys.snapdrop.MainActivity: void onRestart()>
			Changed code in this method:
				@@ -367,7 +353,7 @@ 
				     public void onRestart() {
				         super.onRestart();
				-        if (isServerConnectionLost()) {
				+        if (binding.webview.getUrl() == null || !binding.webview.getUrl().startsWith(baseURL)) {
				             refreshWebsite();
				         }
				     }
		1. <android.app.Activity: void onRestart()>
		2. <com.fmsys.snapdrop.MainActivity: void refreshWebsite()>
		3. - <com.fmsys.snapdrop.MainActivity: boolean isServerConnectionLost()>
			Changed code in this method:
				@@ -344,17 +341,6 @@ 
				-    private boolean isServerConnectionLost() {
				-        return System.currentTimeMillis() - prefs.getLong(getString(R.string.pref_last_server_connection), 0) > 1000 * 60;
				-    }
		4. <android.content.Context: java.lang.String getString(int)>
	- **Edges(Calling relationship between reachable methods)**:
		<0, 1>
		<0, 2>
		- <0, 3>
		<3, 4>
