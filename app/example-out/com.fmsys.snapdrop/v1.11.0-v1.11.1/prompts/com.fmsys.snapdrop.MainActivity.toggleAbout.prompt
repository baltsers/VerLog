**App Description**: Transfer files seamlessly between all your devices - Snapdrop.
**Entry Method Signature**: <com.fmsys.snapdrop.MainActivity: void toggleAbout()>
**Method Line Number(start,end)**: (245, 252)
**Commit Messages between two versions**:
	1. make links clickable in attribution section
	2. add Material Design 3 for the app icon
	3. Update translation_validation.yaml
	4. New Crowdin updates (#216)
	5. Update README.md
	6. some cleanup
	7. New Bugfix Release - v1.11.1
**Partial call graph**:
	- **Nodes(Reachable methods(and their changes) from entry method)**:

		0. <com.fmsys.snapdrop.MainActivity: void toggleAbout()>
			Changed code in this method:
				@@ -243,12 +242,11 @@ 
				     private void toggleAbout() {
				-        if (currentlyAtAboutPage) {
				+        if (binding.webview.getUrl() != null && binding.webview.getUrl().endsWith("#about")) {
				             binding.webview.loadUrl(baseURL + "#");
				         } else {
				             binding.webview.loadUrl(baseURL + "#about");
				         }
				-        currentlyAtAboutPage = !currentlyAtAboutPage;
				     }
